import { Text } from "react-native";

import Layout from "../Layout";

const Loading = () => {
  return (
    <Layout>
      <Text>Searching for countries...</Text>
    </Layout>
  );
};

export default Loading;
